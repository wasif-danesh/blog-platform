name: Storybook to GitHub Pages

on:
  push:
    branches: [ dev ]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write
  contents: read

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with: { run_install: false }

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install workspace deps
        run: pnpm install -w --frozen-lockfile

      # Ensure the UI package has its own node_modules (some runners need this)
      - name: Ensure UI deps
        run: pnpm -C packages/ui install

      # Optional: confirm CLI exists in the UI package context
      - name: Storybook version
        run: pnpm --filter ./packages/ui exec storybook --version

      - name: Build Storybook
        run: pnpm --filter ./packages/ui build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: packages/ui/storybook-static

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
